package simple.stock.market.model.type;

/**
 * @author sergio
 *
 *This class assumes only the two basic stock types
 *
 *Has been decided to keep this an inner class since this values are not used any where else
 */
public enum StockType {

	COMMON,
	
	PREFERRED;
}
